import { ref } from 'vue';

export const projectInterests = ref<string[]>([
    'Website', 'Machine Learning', 'Mobile App'
]);

export const toolsKnown = ref<string[]>([
    'HTML', 'CSS', 'Java', 'Python'
]);

export const toolsDesiredToLearn = ref<string[]>([
    'React', 'Vue', 'Flask'
]);

export const topicInterests = ref<string[]>([
    'Sports', 'Video Games', 'Cats'
]);