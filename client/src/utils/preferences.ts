import { ref } from 'vue';

export const projectInterests = ref([
    'Website', 'Machine Learning', 'Mobile App'
]);

export const toolsKnown = ref([
    'HTML', 'CSS', 'Java', 'Python'
]);

export const toolsDesiredToLearn = ref([
    'React', 'Vue', 'Flask'
]);

export const topicInterests = ref([
    'Sports', 'Video Games', 'Cats'
]);